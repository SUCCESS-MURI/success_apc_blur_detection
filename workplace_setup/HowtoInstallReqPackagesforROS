How to install on ROS
git clone the repo in the catkin_ws/src folder.
copy into model the weights https://drive.google.com/drive/folders/145iWLhZA10qACcsR0gLZ1d9t43EcnBpN?usp=sharing
run
chmod +x blur_detection_ros_main.py
# packages for python that need to be installed 
pip install tensorflow-gpu==2.2.0
pip3 install tensorlayer==2.2.3

cd to catkin_ws

catkin_make
source devel/setup.bash
rosrun success_apc_blur_detection blur_detection_ros_main.py

NOTE: image sizes are different for byu and cmu. change this is blur_detection_ros_main.py height and width comment out the lines for cmu vs byu 
New change : use 256 for real time run
